---
title: "Vercel DNS: SPF, DKIM and DMARC"
sidebarTitle: "Vercel"
description: "Add AhaSend SPF, DKIM and DMARC records in Vercel: find the DNS form, enter the right names and values, then verify your sending domain."
---

import DnsPrerequisites from "/snippets/dns-prerequisites.mdx";
import DnsChecks from "/snippets/dns-check-records.mdx";
import DnsRelated from "/snippets/dns-related.mdx";

AhaSend verifies domains hosted at Vercel when you publish the SPF, DKIM and DMARC records from your domain page; this guide shows where to enter them.

<DnsPrerequisites />

## Open the DNS Form

Open your team’s **Domains** page and select the domain to reach its DNS settings. Choose the type from AhaSend and fill in **Name**, **Type** and **Value**. These steps follow [Vercel's DNS guide](https://vercel.com/docs/domains/managing-dns-records).

This works only when Vercel hosts the authoritative DNS. A website deployed on Vercel can still use another DNS provider. Use the prefix in Name, such as `managed._domainkey`; add each TXT or CNAME record with the form’s **Add** button. You do not need a Vercel email preset.

## Add SPF, DKIM and DMARC

For a zone named `example.com`, use this name mapping. The examples show a common managed-DKIM setup; your dashboard's names and targets take precedence.

| AhaSend record | Type | Name in the provider form | Value |
|---|---|---|---|
| SPF for `example.com` | TXT | leave Name empty | SPF value shown by AhaSend, merged into any existing SPF record |
| `managed._domainkey.example.com` | CNAME | `managed._domainkey` | First DKIM target from your domain page |
| `managed2._domainkey.example.com` | CNAME | `managed2._domainkey` | Second DKIM target from your domain page |
| `_dmarc.example.com` | TXT | `_dmarc` | Your reviewed DMARC policy |

<DnsChecks />

<DnsRelated />
