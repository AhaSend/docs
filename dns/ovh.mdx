---
title: "OVHcloud DNS: SPF, DKIM and DMARC"
sidebarTitle: "OVHcloud"
description: "Add AhaSend SPF, DKIM and DMARC records in OVHcloud: find the DNS form, enter the right names and values, then verify your sending domain."
---

import DnsPrerequisites from "/snippets/dns-prerequisites.mdx";
import DnsChecks from "/snippets/dns-check-records.mdx";
import DnsRelated from "/snippets/dns-related.mdx";

AhaSend verifies domains hosted at OVHcloud when you publish the SPF, DKIM and DMARC records from your domain page; this guide shows where to enter them.

<DnsPrerequisites />

## Open the DNS Form

Open the OVHcloud Control Panel, choose **Web Cloud → Domain names**, select your domain, then open the **DNS zone** tab. Choose the type from AhaSend and fill in **Sub-domain** and **Target**. These steps follow [OVHcloud's DNS guide](https://docs.ovhcloud.com/en/guides/web-cloud/domains/dns-zone-edit).

Choose **Add an entry**, then TXT or CNAME. OVHcloud adds the zone name after the Sub-domain field; review that full name in the confirmation. Do not use the reset-zone action. A CNAME must not share its exact name with another record type.

## Add SPF, DKIM and DMARC

For a zone named `example.com`, use this name mapping. The examples show a common managed-DKIM setup; your dashboard's names and targets take precedence.

| AhaSend record | Type | Name in the provider form | Value |
|---|---|---|---|
| SPF for `example.com` | TXT | leave Sub-domain empty | SPF value shown by AhaSend, merged into any existing SPF record |
| `managed._domainkey.example.com` | CNAME | `managed._domainkey` | First DKIM target from your domain page |
| `managed2._domainkey.example.com` | CNAME | `managed2._domainkey` | Second DKIM target from your domain page |
| `_dmarc.example.com` | TXT | `_dmarc` | Your reviewed DMARC policy |

<DnsChecks />

<DnsRelated />
