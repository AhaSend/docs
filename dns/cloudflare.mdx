---
title: "Cloudflare DNS: SPF, DKIM and DMARC"
sidebarTitle: "Cloudflare"
description: "Add AhaSend SPF, DKIM and DMARC records in Cloudflare: find the DNS form, enter the right names and values, then verify your sending domain."
---

import DnsPrerequisites from "/snippets/dns-prerequisites.mdx";
import DnsChecks from "/snippets/dns-check-records.mdx";
import DnsRelated from "/snippets/dns-related.mdx";

AhaSend verifies domains hosted at Cloudflare when you publish the SPF, DKIM and DMARC records from your domain page; this guide shows where to enter them.

<DnsPrerequisites />

## Open the DNS Form

Select your domain in Cloudflare and open **DNS → Records → Add record**. Choose the type from AhaSend and fill in **Name**, **Content** (TXT) or **Target** (CNAME). These steps follow [Cloudflare's DNS guide](https://developers.cloudflare.com/dns/manage-dns-records/how-to/create-dns-records/).

Set every AhaSend CNAME to **DNS only**, with the grey cloud. A proxied CNAME hides the DNS target AhaSend needs to check. Keep the default TTL. Do not enable CNAME flattening for these verification records.

## Add SPF, DKIM and DMARC

For a zone named `example.com`, use this name mapping. The examples show a common managed-DKIM setup; your dashboard's names and targets take precedence.

| AhaSend record | Type | Name in the provider form | Value |
|---|---|---|---|
| SPF for `example.com` | TXT | `@` | SPF value shown by AhaSend, merged into any existing SPF record |
| `managed._domainkey.example.com` | CNAME | `managed._domainkey` | First DKIM target from your domain page |
| `managed2._domainkey.example.com` | CNAME | `managed2._domainkey` | Second DKIM target from your domain page |
| `_dmarc.example.com` | TXT | `_dmarc` | Your reviewed DMARC policy |

<DnsChecks />

<DnsRelated />
