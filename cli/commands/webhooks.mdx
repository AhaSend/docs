---
title: "ahasend webhooks: Manage and Test Webhooks"
description: "Create, update, and delete AhaSend email event webhooks from the CLI, and test them locally with webhooks listen (forward to localhost) and webhooks trigger."
icon: webhook
sidebarTitle: "webhooks"
---

AhaSend CLI `webhooks` commands let you forward and trigger email event webhooks; this page lists their arguments and flags. For the complete workflow, use the [webhook testing guide](/cli/webhook-testing).

`webhooks get`, `webhooks update`, `webhooks delete` and `webhooks trigger` take the resource ID as their positional argument. `webhooks create`, `webhooks list` and `webhooks listen` use the flags below. For dashboard setup, see [the webhooks guide](/integrations/webhooks).

Create and update use short event keys: `reception`, `delivered`, `transient_error`, `failed`, `bounced`, `suppressed`, `opened`, `clicked`, `suppression_created`, and `dns_error`. Listen and trigger use full event types such as `message.delivered`. See the [event reference](/api-reference/webhooks).

## Command Flags

These flags come from the current CLI. All commands also accept the [global flags](/cli/configuration#global-flags).

### `webhooks create`

| Flag | Description and default |
|---|---|
| `--all-events` | Listen for all available event types |
| `--disabled` | Create webhook in disabled state |
| `--domains strings` | Limit webhook to specific domains |
| `--events strings` | Comma-separated list of event types to listen for |
| `--interactive` | Force interactive mode even when flags are provided |
| `--name string` | Webhook name (required) |
| `--non-interactive` | Skip interactive prompts (use flag values only) |
| `--scope string` | Webhook scope (optional) |
| `--url string` | Webhook URL (required) |

### `webhooks delete`

| Flag | Description and default |
|---|---|
| `--force` | Skip confirmation prompt |

### `webhooks get`

This command has no command-specific flags. See [global flags](/cli/configuration#global-flags).

### `webhooks list`

| Flag | Description and default |
|---|---|
| `--cursor string` | Pagination cursor for next page |
| `--enabled` | Show only enabled webhooks |
| `--limit int32` | Maximum number of webhooks to return |

### `webhooks listen`

| Flag | Description and default |
|---|---|
| `--events strings` | Filter specific events (client-side) |
| `--forward-to string` | Local endpoint to forward events to |
| `--skip-verify` | Skip SSL certificate verification for local endpoints when forwarding events |
| `--slim-output` | Slim down the payload for printing to the console |
| `--webhook-id string` | Use existing webhook instead of creating temporary one |

### `webhooks trigger`

| Flag | Description and default |
|---|---|
| `--all-events` | Trigger all available event types |
| `--events strings` | Event types to trigger |

### `webhooks update`

| Flag | Description and default |
|---|---|
| `--all-events` | Enable all available event types |
| `--clear-domains` | Clear all domain restrictions |
| `--disable` | Disable the webhook |
| `--domains strings` | Set domain restrictions (replaces existing) |
| `--enable` | Enable the webhook |
| `--events strings` | Set event types (replaces existing) |
| `--name string` | Update webhook name |
| `--no-events` | Disable all event types |
| `--scope string` | Update webhook scope |
| `--url string` | Update webhook URL |

## Related Guides

- [Install the CLI](/cli/installation), [CLI authentication](/cli/authentication), [Webhook setup](/integrations/webhooks).
- [Event reference](/api-reference/webhooks), [Local webhook testing](/cli/webhook-testing), [Signature verification](/api-reference/webhooks/security).
- [Retry policy](/api-reference/webhooks/retry-policy), [CLI configuration and output](/cli/configuration), [API authentication](/api-reference/authentication).
- [Permission scopes](/api-reference/scopes), [Suppression durations](/bounces#suppression-duration).
