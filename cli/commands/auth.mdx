---
title: "ahasend auth: Log In and Manage Profiles"
description: "Log the AhaSend CLI in with an API key and account ID, check auth status, and switch between profiles for production, staging, and development."
icon: user-shield
sidebarTitle: "auth"
---

AhaSend CLI `auth` commands let you log in and switch saved profiles; this page lists their arguments and flags. For the complete workflow, use the [authentication guide](/cli/authentication).

`auth logout` takes a profile name; `auth switch` takes the profile to activate. `auth login` and `auth status` use the profile flags below. Credential storage and CI authentication are covered in [CLI authentication](/cli/authentication).

## Command Flags

These flags come from the current CLI. All commands also accept the [global flags](/cli/configuration#global-flags).

### `auth login`

| Flag | Description and default |
|---|---|
| `--account-id string` | AhaSend Account ID |
| `--api-key string` | AhaSend API key (not recommended, use interactive prompt) |
| `--api-url string` | AhaSend API URL (default "https://api.ahasend.com") |
| `--profile string` | Profile name to save credentials under |

### `auth logout`

| Flag | Description and default |
|---|---|
| `--all` | Logout from all profiles |

### `auth status`

| Flag | Description and default |
|---|---|
| `--all` | Show status for all profiles |
| `--profile string` | Show status for specific profile |

### `auth switch`

This command has no command-specific flags. See [global flags](/cli/configuration#global-flags).

## Related Guides

- [Install the CLI](/cli/installation), [CLI authentication](/cli/authentication), [REST sending guide](/send-api/send-email).
- [SMTP settings](/smtp), [Node.js SDK](/guides/nodejs-sdk), [Go SDK](/guides/go-sdk).
- [CLI configuration and output](/cli/configuration), [API authentication](/api-reference/authentication), [Permission scopes](/api-reference/scopes).
