Before sending, [verify your domain](/domains) and [create an SMTP credential](/smtp/credentials). Use a [sandbox credential](/send-api/sandbox) for these examples so no email is delivered. Keep the username and password in your server's secret store.
