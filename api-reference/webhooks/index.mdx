---
title: "Webhooks API"
sidebarTitle: "Introduction"
description: "Real-time HTTP callbacks for email events following the Standard Webhooks specification"
icon: "webhook"
---

<Note>
**New to webhooks?** Start with our [comprehensive webhooks guide](/integrations/webhooks) to learn how to create and configure webhooks in your dashboard before diving into the API details.
</Note>

Webhooks are user-defined HTTP callbacks that notify your application in real-time when specific events occur in AhaSend. When an email is delivered, bounced, opened, or other events happen, AhaSend sends a POST request to your configured endpoint with detailed event data.

## Overview

AhaSend uses Standard Webhooks headers and format. Its HMAC key is the full secret as literal UTF-8 bytes, not base64-decoded. Use a raw-key verifier; see [security](/api-reference/webhooks/security). Features include:

- **Real-time notifications** for email events (delivery, bounce, open, click, suppression)
- **Secure payload delivery** with HMAC signature verification
- **Automatic retries** with 6 attempts over about 16 minutes, including the first attempt
- **Idempotency support** via unique webhook-id headers

## Event Types

| Event type | Filter flag | Fires when | Payload |
|---|---|---|---|
| `message.reception` | `on_reception` | A message is accepted | [Reference](/api-reference/webhooks/message-reception) |
| `message.delivered` | `on_delivered` | The recipient server accepts the message | [Reference](/api-reference/webhooks/message-delivered) |
| `message.transient_error` | `on_transient_error` | A delivery attempt has a temporary failure | [Reference](/api-reference/webhooks/message-transient_error) |
| `message.failed` | `on_failed` | Message delivery fails | [Reference](/api-reference/webhooks/message-failed) |
| `message.bounced` | `on_bounced` | The recipient server rejects the message | [Reference](/api-reference/webhooks/message-bounced) |
| `message.suppressed` | `on_suppressed` | A suppression prevents sending | [Reference](/api-reference/webhooks/message-suppressed) |
| `message.opened` | `on_opened` | The tracking image is requested | [Reference](/api-reference/webhooks/message-opened) |
| `message.clicked` | `on_clicked` | A tracked link is requested | [Reference](/api-reference/webhooks/message-clicked) |
| `suppression.created` | `on_suppression_created` | A suppression is created | [Reference](/api-reference/webhooks/suppression-created) |
| `domain.dns_error` | `on_dns_error` | A sending domain has a DNS error | [Reference](/api-reference/webhooks/domain-dns_error) |

Inbound routing has a separate [`message.routing` event](/api-reference/routes/route-message).

## Suppression Duration

| Cause | Duration |
|---|---|
| Most bounces | 30 days |
| Temporary delivery failures | 7 days |
| Message expires after repeated attempts | 15 days |
| Spam complaint | Effectively permanent |
| Created through the API | Caller supplies the required `expires_at` |

## Payload Structure

All webhook events use a consistent JSON structure:

Events contain `type`, `webhook_id`, `timestamp`, and an event-specific `data` object. The payload `webhook_id` identifies the configured endpoint; the `webhook-id` header identifies the delivery across retries. Inbound routes use `route_id` instead of `webhook_id`. Use the payload reference for each event above.

## Security Headers

Every webhook request includes three security headers for verification:

- `webhook-id`: Unique event identifier for idempotency
- `webhook-timestamp`: Unix timestamp when the webhook was sent
- `webhook-signature`: HMAC signature for payload verification

## Getting Started

<CardGroup cols={2}>
  <Card title="Create Webhooks" icon="plus" href="/integrations/webhooks">
    Learn how to configure webhooks in your dashboard and handle events
  </Card>
  <Card title="Security" icon="shield" href="/api-reference/webhooks/security">
    Implement signature verification and security best practices
  </Card>
  <Card title="Retry Policy" icon="arrows-rotate" href="/api-reference/webhooks/retry-policy">
    Understand webhook delivery guarantees and retry behavior
  </Card>
</CardGroup>
