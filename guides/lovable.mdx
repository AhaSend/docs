---
title: "How to Send Email from a Lovable App"
sidebarTitle: "Lovable"
description: "Add transactional email to a Lovable app: send from a Supabase Edge Function with the AhaSend SDK, with your key kept in Lovable Cloud secrets."
keywords: ["lovable app send email", "lovable send email", "lovable ahasend", "lovable transactional email", "lovable supabase edge function email"]
icon: "heart"
---

Ask [Lovable](https://lovable.dev) for email and it will build something that shows a success toast in the preview. Whether that toast means a delivered message or a published API key is the part you cannot see from there.

## Prerequisites

- A Lovable project with Lovable Cloud enabled
- An [AhaSend account](https://dash.ahasend.com/user/register) with a [verified sending domain](/domains)
- An [API key](/send-api/credentials) with a send scope, plus your account ID

Lovable cannot verify your sending domain or create your credentials. Those steps happen in the [dashboard](https://dash.ahasend.com) and are quick: the [quickstart](/quickstart) has both. Scope the key to `messages:send:{your-domain}` rather than `messages:send:all`, so if the key ever leaks the blast radius is one domain's outbound mail instead of your whole account.

## Store the Key as a Secret

Anything the browser can read, a visitor can read, so the key never goes in frontend code or in a `VITE_`-prefixed variable, which ships to the browser by design.

<Warning>
  If you ever find yourself typing your API key into a variable with a `VITE_` prefix, stop. That one is public. Rotate the key before continuing.
</Warning>

In your Lovable project, enable Lovable Cloud in the project's backend settings. That gives your app server-side functions and a place to keep secrets. Then open the Secrets section and add two values:

```bash
AHASEND_API_KEY=aha-sk-...
AHASEND_ACCOUNT_ID=your-account-uuid
```

Lovable encrypts these and injects them into your Edge Functions at runtime. They are not part of your frontend bundle, they are not in your project's `.env`, and they do not show up in the published site. That is the whole point.

## Prompt Lovable for a Backend Send

Lovable Cloud runs backend code as Supabase Edge Functions on Deno, and [`@ahasend/sdk`](https://www.npmjs.com/package/@ahasend/sdk) supports Deno from 0.2.0 onward, so import it there with an `npm:` specifier and let it handle retries, idempotency, and typed errors. Deno has no `process.env`, so read the secrets with `Deno.env.get()` and pass them to the constructor.

Ask precisely. The words that matter most are *Edge Function*: without them, Lovable may call the email API from the client. Spell out the architecture:

> "When a user signs up, send a welcome email through AhaSend. Do it in a Supabase Edge Function, never from the frontend. Import the client with `import { AhaSendClient } from 'npm:@ahasend/sdk'` and use version 0.2.0 or later. Read the API key and account ID from the secrets `AHASEND_API_KEY` and `AHASEND_ACCOUNT_ID` using `Deno.env.get()` and pass them to the constructor. Treat HTTP 202 as success. The frontend should call the Edge Function, not AhaSend directly."

The function it generates should look close to this:

```ts supabase/functions/send-welcome-email/index.ts
import { AhaSendClient, AhaSendAPIError } from "npm:@ahasend/sdk";

const ahasend = new AhaSendClient({
  apiKey: Deno.env.get("AHASEND_API_KEY")!,
  accountId: Deno.env.get("AHASEND_ACCOUNT_ID")!,
});

Deno.serve(async (req) => {
  const { email, name } = await req.json();

  try {
    const result = await ahasend.messages.send({
      from: { email: "hello@yourdomain.com", name: "Your App" },
      recipients: [{ email, name }],
      subject: "Welcome aboard",
      html_content: `<p>Thanks for signing up, ${name}.</p>`,
      text_content: `Thanks for signing up, ${name}.`,
      sandbox: true, // flip to false when you're ready to send for real
    });

    const rejected = result.data.filter((r) => r.status === "error");

    // 200 here is this function's reply to your frontend, not AhaSend's.
    return new Response(JSON.stringify({ queued: result.data.length - rejected.length }), {
      status: 200,
    });
  } catch (err) {
    if (err instanceof AhaSendAPIError) {
      console.error(`AhaSend error ${err.status} (request ${err.requestId})`);
      return new Response(JSON.stringify({ error: "Failed to send email" }), {
        status: 502,
      });
    }
    throw err;
  }
});
```

Check two details in review. The secrets are read at runtime with `Deno.env.get()`, so the key lives in the function and nowhere else. And the client is created once at module scope, not per request, which keeps its retry and idempotency state.

AhaSend answers a send with **202**, not 200, because delivery is asynchronous: the API has accepted and queued your message rather than finished delivering it. The body is multi-status, so `result.data` carries one entry per recipient, and an individual recipient can come back `status: "error"` with a null `id`, a suppressed address for example, while the promise resolves. Check every entry, not just the first.

## Verify Where the Key Landed

Lovable's preview will not catch this mistake for you. Use Lovable's code view to confirm two things.

<Steps>
  <Step title="The key is referenced only inside the Edge Function">
    Via `Deno.env.get()`, and nowhere else. If you see your key, anything starting with `aha-sk-`, or a `VITE_AHASEND...` variable anywhere in the frontend, send Lovable back: "Move the AhaSend call into the Edge Function and remove the key from the frontend entirely." Then rotate the key.
  </Step>
  <Step title="The frontend calls your Edge Function">
    Which then calls AhaSend, not the other way around. The browser should be talking to your own backend, never directly to `api.ahasend.com`.
  </Step>
</Steps>

<Tip>
  If you are not comfortable reading the code, ask Lovable directly: "Is my AhaSend API key exposed anywhere in the frontend bundle?" Then verify its answer against the two checks above rather than taking the success toast at face value.
</Tip>

## Test in Sandbox Mode

Keep the `sandbox: true` in the code above while you build. In sandbox mode AhaSend runs your message through the full validation and queuing pipeline, fires all the same webhooks, shows it in your dashboard logs, and then stops just short of delivery. It costs nothing and it cannot email a real customer by accident.

Sandbox also lets you rehearse the unhappy paths. Add `"sandbox_result": "bounce"` and AhaSend simulates a hard bounce so you can see how your app reacts. `"defer"`, `"fail"`, and `"suppress"` cover the other outcomes, and the full list is in the [sandbox mode guide](/send-api/sandbox). Run through them once, confirm you get a clean 202 on the happy path, and only then change `sandbox` to `false`. Read the flag from a secret too, so you can toggle real sending without touching code.

## Going Further

- **Templating**: pass `substitutions` per recipient and use `{{ variable }}` in the subject or body.
- **Batch sends**: `recipients` accepts up to 100 entries, each of which gets its own message.
- **Webhooks**: AhaSend signs every webhook with the Standard Webhooks scheme, and `WebhookVerifier` from `@ahasend/sdk/webhooks` verifies it on Deno too. Remember to `await verifier.parse()`, which is async from 0.2.0. The [Cloudflare Workers guide](/guides/cloudflare-workers) shows the same pattern on another edge runtime.
- **Attachments, scheduling, tags**: all available on the same endpoint. See the [API reference](/api-reference).
- **Moving the send to a Node server**: the same SDK code runs there. The [Express guide](/guides/express) covers that path.
- Building in a different AI tool? See [v0](/guides/v0) and [Bolt.new](/guides/bolt-new).

## Troubleshooting

<AccordionGroup>
  <Accordion title="Error importing @ahasend/sdk in the Edge Function">
    You are on an SDK version older than 0.2.0, which is where Deno and edge runtime support landed. Pin `npm:@ahasend/sdk` to 0.2.0 or later and redeploy the function.
  </Accordion>
  <Accordion title="401 from api.ahasend.com">
    The secret is not set, or the function was deployed before the secret existed. Re-check the Secrets section in Lovable Cloud, redeploy the function, and confirm the key has a send scope.
  </Accordion>
  <Accordion title="The frontend gets a CORS error calling the Edge Function">
    Supabase Edge Functions need to answer the preflight `OPTIONS` request with `Access-Control-Allow-Origin` and `Access-Control-Allow-Headers`. Ask Lovable to add CORS headers to the function. This is a function concern, not an AhaSend one.
  </Accordion>
  <Accordion title="400 error mentioning the from address">
    The `from` address must belong to a verified sending domain on your account. Check domain status in the dashboard.
  </Accordion>
</AccordionGroup>
